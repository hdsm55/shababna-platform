# Environment variables required for production and staging

# Server
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# CORS / URLs
CLIENT_URL=
FRONTEND_URL=
BACKEND_URL=
ALLOWED_ORIGINS=

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Database
DATABASE_URL=
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=
PGSSL=true
PG_MAX=20
PG_MIN=0
PG_IDLE_TIMEOUT=30000
PG_CONN_TIMEOUT=20000
PG_STATEMENT_TIMEOUT=30000
PG_QUERY_TIMEOUT=30000
PG_APP_NAME=shababna-platform

# Email
EMAIL_ENABLED=false
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=no-reply@shababna.org
EMAIL_FROM_NAME=Shababna Global
FROM_EMAIL=
FROM_NAME=
REPLY_TO_EMAIL=
ADMIN_EMAILS=

# Stripe / Iyzico
STRIPE_SECRET_KEY=
IYZICO_API_KEY=
IYZICO_SECRET_KEY=
IYZICO_URI=https://sandbox-api.iyzipay.com

# Frontend (Vite)
VITE_API_URL=
VITE_PRODUCTION_API_URL=
